#include <stdio.h>

int main(){

/**
 * @brief We need to create a calc 
 * we need 2var  
 * then we need to do calc like + - / etc so need a method to fugure it out
 * 
 */

    int num1;
    int num2;

    printf("first number");

    scanf("%d",&num1);

    printf("second number");

    scanf("%d",&num2);
    //printf("which operation u want to do + - ");
 
    
    printf("%d + %d =%d",num1,num2,num1+num2);
}